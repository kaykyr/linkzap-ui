{
    "name": "linkzap-ui",
    "private": true,
    "workspaces": [
        "packages/*",
        "./packages/*"
    ],
    "scripts": {
        "build": "cd ./packages/react && npm run build && cd ../tokens && npm run build",
        "dev": "cd ./packages/docs && npm run dev"
    }
}
