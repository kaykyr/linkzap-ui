import type { StoryObj, Meta } from '@storybook/react'
import { Button } from '@linkzap-ui/react'

export default {
    title: 'Button',
    component: Button,
} as Meta

export const Default: StoryObj = {}
